# .cgw.conf -- Auto-generated by configure.sh on Sat Jul  4 18:09:49 EDT 2026
# Edit as needed. See cgw.conf.example for all options.
# This file is git-ignored (.cgw.conf in .gitignore).

# Branch configuration
# (target branch is auto-detected at runtime; not stored here -- see _config.sh)
CGW_SOURCE_BRANCH="development"

# Local-only files (space-separated; never committed)
CGW_LOCAL_FILES="CLAUDE.md SESSION_LOG.md logs/"

# Lint configuration (auto-detected)
CGW_LINT_CMD="ruff"
CGW_LINT_CHECK_ARGS="check {files}"
CGW_LINT_FIX_ARGS="check --fix {files}"
CGW_LINT_EXCLUDES="--extend-exclude logs"
CGW_FORMAT_CMD="ruff"
CGW_FORMAT_CHECK_ARGS="format --check {files}"
CGW_FORMAT_FIX_ARGS="format {files}"
CGW_FORMAT_EXCLUDES="--exclude logs"

# Typecheck configuration (auto-detected)
CGW_TYPECHECK_CMD="pyrefly"
CGW_TYPECHECK_CHECK_ARGS="check"
CGW_TYPECHECK_EXCLUDES=""

# Commit message prefix extras (pipe-separated, e.g. "cuda|tensorrt")
CGW_EXTRA_PREFIXES=""

# Docs CI pattern (empty = skip; set to enable doc filename validation)
# Example: CGW_DOCS_PATTERN="^(README\.md|.*_GUIDE\.md|.*_REFERENCE\.md)$"
CGW_DOCS_PATTERN=""

# Dev-only files warning for cherry-pick (space-separated; empty = skip)
# Example: CGW_DEV_ONLY_FILES="tests/ pytest.ini"
CGW_DEV_ONLY_FILES=""

# Markdown lint (empty = disabled). ARGS = flags/exclusions only;
# PATHS = whole-repo audit target (commit gate scopes to staged *.md).
# CGW_MARKDOWNLINT_CMD="markdownlint-cli2"
# CGW_MARKDOWNLINT_ARGS="!CLAUDE.md !MEMORY.md"
# CGW_MARKDOWNLINT_PATHS="**/*.md"

# Allow merge/cherry-pick to carry CGW_LOCAL_FILES into shared history
# (guard aborts non-interactively when 0)
# CGW_ALLOW_LOCAL_FILES_IN_MERGE="0"

# Remove tests/ from target branch if gitignored (0=disabled, 1=enabled)
CGW_CLEANUP_TESTS="0"
