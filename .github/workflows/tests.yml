name: Tests

on:
  push:
    branches: [development, main, master, 'claude/**']
    paths:
      - 'src/cuda_link/**'
      - 'tests/**'
      - 'pyproject.toml'
      - '.github/workflows/tests.yml'
      - 'td_exporter/**'
      - 'scripts/sync_td_wrapper.py'
      - 'native/**'
  pull_request:
    paths:
      - 'src/cuda_link/**'
      - 'tests/**'
      - 'pyproject.toml'
      - '.github/workflows/tests.yml'
      - 'td_exporter/**'
      - 'scripts/sync_td_wrapper.py'
      - 'native/**'
  workflow_dispatch:

jobs:
  pytest:
    name: pytest (no GPU) — Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.10', '3.11', '3.12']

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install test dependencies
        # pytest-randomly enables the randomized test ordering configured in
        # pyproject.toml addopts (--randomly-dont-reset-seed). pytest-cov matches
        # the dev extra for parity. numpy + typing_extensions are required by the
        # no-GPU suite. torch is a best-effort CPU install: the few tests that
        # need it use pytest.importorskip, so the suite stays green on any Python
        # version without a CPU wheel. cupy is omitted (CUDA-only; no test marked
        # "not requires_cuda" needs it).
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-randomly pytest-cov typing_extensions numpy
          pip install torch --index-url https://download.pytorch.org/whl/cpu \
            || echo "::notice::torch CPU wheel unavailable for Python ${{ matrix.python-version }} — torch-dependent tests will skip"

      - name: Run no-GPU test suite
        # pytest reads pythonpath = ["src", "tests"] from pyproject.toml, so the
        # package and test helpers import without an editable install.
        # --cov enforces the fail_under gate in [tool.coverage.report] (pyproject.toml).
        # --cov-report=xml produces coverage.xml for Codecov upload below.
        run: pytest tests/ -m "not requires_cuda" --cov=cuda_link --cov-report=term-missing --cov-report=xml -q

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: ./coverage.xml
          flags: main
          token: ${{ secrets.CODECOV_TOKEN }}
          # fail_ci_if_error=false: keep CI green while the repo is being activated on
          # codecov.io (the upload is a no-op until CODECOV_TOKEN secret is set).
          fail_ci_if_error: false

      - name: Upload coverage report artifact
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: coverage-main-${{ matrix.python-version }}
          path: coverage.xml
          retention-days: 14

  native-tests:
    name: native pytest (no GPU)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: pip

      - name: Install dependencies
        # cuda-link is a runtime dependency of cuda_link_native (for
        # CUDARuntimeAPI.cudart_event_query_fn_ptr() etc.). Install it in
        # editable mode first so the native package can import it.
        # pytest-randomly + pytest-cov match native/pyproject.toml's dev config.
        # The native _native_waiter extension requires Windows + a loaded CUDA
        # runtime; FakeWaitBackend replaces it for all tests not marked
        # requires_native.
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install pytest pytest-randomly pytest-cov numpy

      - name: Run no-GPU native test suite
        # pytest reads [tool.pytest.ini_options] from native/pyproject.toml when
        # run from the native/ directory (rootdir = native/).
        # --cov enforces the fail_under gate in [tool.coverage.report] (native/pyproject.toml).
        # coverage.xml is written to native/ (pytest runs from there via cd).
        run: cd native && pytest tests -m "not requires_native" --cov=cuda_link_native --cov-report=term-missing --cov-report=xml -q

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: ./native/coverage.xml
          flags: native
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false

      - name: Upload coverage report artifact
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: coverage-native
          path: native/coverage.xml
          retention-days: 14
